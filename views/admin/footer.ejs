</main><!-- End #main -->
<% if(is_login) { %>

<!-- ======= Footer ======= -->
<footer id="footer" class="footer fixed-bottom p-1  bg-white">
  <div class="col-md-12 text-center copyright">
    <p>	&#169; @ All Rights Reserved  Design by <a href="https://www.a2zithub.org/" target="_blank">A2Z IT HUB PVT LTD</a></p>
  </div>
</footer><!-- End Footer -->

<a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>

<!-- Vendor JS Files -->
<script src="/admin/vendor/apexcharts/apexcharts.min.js"></script>
<script src="/admin/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
<script src="/admin/vendor/chart.js/chart.umd.js"></script>
<script src="/admin/vendor/echarts/echarts.min.js"></script>
<script src="/admin/vendor/quill/quill.js"></script>
<script src="/admin/vendor/simple-datatables/simple-datatables.js"></script>
<script src="/admin/vendor/tinymce/tinymce.min.js"></script>
<script src="/admin/vendor/php-email-form/validate.js"></script>



<!-- Template Main JS File -->
<script src="/admin/js/main.js"></script>


<!-- Bootstrap JS (for any future functionality you need) -->
<!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script> -->

<script>
// Copy Text Function
function copyText() {
    const content = document.getElementById('content').innerText;
    navigator.clipboard.writeText(content).then(() => {
        alert('Text copied to clipboard!');
    });
}

// Download CSV Function
function downloadCSV() {
    const content = document.getElementById('content').innerText;
    const rows = content.split('\n').map(row => row.split(' '));
    let csv = 'data:text/csv;charset=utf-8,';
    rows.forEach(row => {
        csv += row.join(',') + '\n';
    });

    const link = document.createElement('a');
    link.setAttribute('href', encodeURI(csv));
    link.setAttribute('download', 'export.csv');
    document.body.appendChild(link);
    link.click();
}

// Download Excel Function
function downloadExcel() {
    const content = document.getElementById('content').innerText;
    const rows = content.split('\n').map(row => row.split(' '));
    const ws = XLSX.utils.aoa_to_sheet(rows);
    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');

    const link = document.createElement('a');
    link.href = URL.createObjectURL(new Blob([wb], {type: 'application/octet-stream'}));
    link.download = 'export.xlsx';
    link.click();
}

// Download PDF Function
function downloadPDF() {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF();

    doc.text(document.getElementById('content').innerText, 10, 10);
    doc.save('export.pdf');
}

// Print Page Function
function printPage() {
    window.print();
}

</script>










<% } %>


  

</body>

</html>